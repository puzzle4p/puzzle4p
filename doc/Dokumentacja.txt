Background:
  Background() - konstruktor przyjmuj¹cy dwa parametry. G³ówne okno i string ze œcie¿k¹ do obrazu. Wyœwietla w g³ównym oknie wczytany obraz jako t³o.
  
Menu:
  Menu() - konstruktor przyjmuj¹cy dwa parametry. G³ówne okno oraz string, który odpowiada za adres do obrazu. Funkcja ta tworzy nowy obiekt Background, do którego przekazuje parametry.
  onMouseDown() - funkcja przyjmuj¹ca dwa parametry, wspó³rzêdne X i Y aktualnego po³o¿enia wskaŸnika myszy podczas gdy wciœniêty jest przycisk w stanie Menu.
  
stateManager:
  changeState() - funkcja z jednym argumentem, który przyjmuje wartoœæ enum global_States. Przejmuje argument i zmienia current_state na podany w argumencie.
  onMouseDown() - funkcja przyjmuj¹ca dwa parametry, wspó³rzêdne X i Y aktualnego po³o¿enia wskaŸnika myszy podczas gdy wciœniêty jest przycisk. A nastêpnie przekazuje te dane do odpowiedniej funkcji w zale¿noœci jaki jest current_state. Np. je¿eli Menu to uruchomi siê funkcja z klasy Menu.
  addToMap() - funkcja przyjmuje dwa parametry. Do mapy enumMapToStates przypisuje wskaŸnik do obiektu klasy i odpowiadaj¹ca jemu wartoœæ enum global_State.
  
Util:
  isPointInsideRect() - funkcja przyjmuje 6 parametrów typu int. Punkt X i Y aktualnego po³o¿enia wskaŸnika myszy, po³o¿enie œrodka obiektu (np. buttona) oraz jego wymiary. Nastêpnie zwraca wartoœæ true or false, w zale¿noœci czy wskaŸnik myszy znajduje siê na tym obiekcie, czy te¿ jest po za nim.